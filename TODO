TODOs:

- Add confirmation to delete history (with force option)
- Scan should handle off-line switches gracefully
- Add debug global option
- Correct indenting in list_switches() SQL
- Allow list_switchports to filter
- Allow list_macs to filter
- Error out if someone modify_switch on an invalid switch
- Error out if someone modify_switchport on an invalid switchport
- Allow friendly way of specifying switchport (switch name/descr + port name/descr)
- --terse and --long shouldn't both be allowed
- Only display full usage if --help is passed in, otherwise keep it short.
- Invalid options should generate friendly error

